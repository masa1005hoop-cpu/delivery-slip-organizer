# 納品書整理ツール — Windows セットアップ手順書

この手順書どおりに進めれば、AIエージェントがなくてもセットアップできます。
**使うソフトはすべて無料**です（後述）。

---

## 全体の流れ

```
1. Python をインストール
2. Tesseract OCR（文字認識）をインストール
3. Poppler（PDF変換）をインストール
4. ツールのセットアップを実行
5. 顧客フォルダを作成
6. ツールを起動
```

所要時間の目安：**30〜45分**（初回のみ）

---

## 使うソフトは無料ですか？

**はい、すべて無料です。**

| ソフト | 用途 | 費用 |
|--------|------|------|
| Python | プログラムの実行 | 無料 |
| Tesseract OCR | スキャンPDFの文字認識 | 無料（オープンソース） |
| Poppler | PDFを画像に変換 | 無料（オープンソース） |
| 納品書整理ツール本体 | 分割・リネーム・振り分け | 無料 |

有料の契約やクレジットカード登録は不要です。

---

## 1. Python 3 をインストール

### 手順

1. ブラウザで https://www.python.org/downloads/ を開く
2. 「Download Python 3.x.x」をクリックしてダウンロード
3. ダウンロードした `.exe` をダブルクリック
4. **重要：** 最初の画面で **「Add python.exe to PATH」にチェック** を入れる
5. 「Install Now」をクリック
6. インストール完了後、「Close」をクリック

### 確認

1. キーボードの `Windows` キー + `R` を押す
2. `cmd` と入力して Enter
3. 黒い画面（コマンドプロンプト）で以下を入力：

```cmd
python --version
```

`Python 3.x.x` と表示されればOKです。

---

## 2. Tesseract OCR をインストール

Tesseract は、スキャンPDFから文字を読み取るソフトです。

### 手順

1. ブラウザで以下を開く：  
   https://github.com/UB-Mannheim/tesseract/wiki
2. 「tesseract-ocr-w64-setup-xxx.exe」（64bit版）をダウンロード
3. ダウンロードした `.exe` をダブルクリック
4. インストール中の選択肢：
   - **Additional language data** で **Japanese** にチェックを入れる（日本語OCRに必要）
   - インストール先はそのままでOK（通常 `C:\Program Files\Tesseract-OCR`）
5. インストール完了

### 確認

コマンドプロンプトを**新しく開き**、以下を入力：

```cmd
tesseract --version
```

バージョンが表示されればOKです。

表示されない場合：
- PCを一度再起動してから再度試す
- または、環境変数 PATH に `C:\Program Files\Tesseract-OCR` を追加（下記「PATHの追加方法」参照）

---

## 3. Poppler をインストール

Poppler は、PDFを画像に変換するために使います。

### 手順

1. ブラウザで以下を開く：  
   https://github.com/oschwartz10612/poppler-windows/releases
2. 最新版の `Release-xxx.zip` をダウンロード
3. ZIPファイルを右クリック →「すべて展開」
4. 展開したフォルダを `C:\poppler` に移動（名前を `poppler` に変更してもOK）
5. **環境変数 PATH に追加**（次のセクション）

### PATHの追加方法

1. `Windows` キーを押して「環境変数」と入力
2. 「システム環境変数の編集」をクリック
3. 「環境変数」ボタンをクリック
4. 「ユーザー環境変数」の「Path」を選択 →「編集」
5. 「新規」をクリック
6. 以下を入力（poppler を別の場所に置いた場合はそのパスに合わせる）：

```
C:\poppler\Library\bin
```

7. 「OK」を3回クリックして閉じる
8. **PCを再起動**（PATHを反映させるため）

### 確認

コマンドプロンプトを新しく開き：

```cmd
pdftoppm -h
```

ヘルプが表示されればOKです。

---

## 4. ツールのセットアップ

### 手順

1. `delivery-slip-organizer` フォルダをPCにコピーする  
   （USBメモリ、共有ドライブ、ZIPファイルなど）
2. コマンドプロンプトを開く
3. フォルダに移動（パスは実際の場所に合わせて変更）：

```cmd
cd C:\Users\あなたの名前\Desktop\delivery-slip-organizer
```

4. セットアップを実行：

```cmd
python setup.py
```

5. 以下のように表示されれば成功：

```
✓ Python3
✓ Tesseract OCR
✓ 日本語OCR (jpn)
✓ poppler (PDF→画像変換)
✓ フォルダ: 受け取り/
...
セットアップOK！
```

---

## 5. 顧客フォルダを作成

エクスプローラーで `delivery-slip-organizer\納品書\` を開き、  
PDF内の「顧客名」と**同じ名前**のフォルダを作成します。

例：

```
納品書\
  ○○株式会社\
  △△商店\
  株式会社サンプル\
```

---

## 6. ツールを起動

コマンドプロンプトで、プロジェクトフォルダに移動してから：

```cmd
.venv\Scripts\python run.py
```

以下のように表示されれば起動成功です：

```
納品書整理ツールを起動しました。
監視中: ...\受け取り
PDFを「受け取り」フォルダに入れると自動で処理します。
```

**この黒い画面は閉じないでください。** 閉じると監視が止まります。

---

## 使い方（日常）

1. ツールを起動した状態にする（上記 `run.py`）
2. 複数枚入りのPDFを `受け取り\` フォルダにドラッグ＆ドロップ
3. 自動で分割・リネーム・振り分けされる
4. 結果は `納品書\顧客名\年\月\` に保存される

### 1回だけ処理したい場合

```cmd
.venv\Scripts\python run.py --once
```

---

## よくあるエラーと対処法

| エラー・症状 | 原因 | 対処 |
|--------------|------|------|
| `python` が認識されない | PythonがPATHに入っていない | Pythonを再インストールし「Add to PATH」にチェック |
| `tesseract` が認識されない | PATH未設定 | `C:\Program Files\Tesseract-OCR` をPATHに追加、PC再起動 |
| 日本語が読めない | 日本語データ未インストール | Tesseractを再インストールし Japanese にチェック |
| PDF変換エラー | Poppler未設定 | `C:\poppler\Library\bin` をPATHに追加、PC再起動 |
| 顧客名フォルダが見つからない | フォルダ名不一致 | `納品書\` 内のフォルダ名をPDF内の顧客名と一致させる |
| 日付・届け先が読めない | OCR精度 | スキャン解像度を上げる。`config.yaml` のラベル名を確認 |

---

## 設定のカスタマイズ

納品書のレイアウトに合わせて `config.yaml` を編集できます。

| 設定項目 | 説明 | デフォルト |
|----------|------|------------|
| `labels.delivery_to` | 届け先のラベル名 | 届け先 |
| `labels.customer` | 顧客名のラベル名 | 顧客名 |
| `filename.delivery_suffix` | 届け先の末尾 | 家 |
| `ocr.dpi` | 読み取り精度（高いほど遅い） | 300 |

メモ帳で `config.yaml` を開いて編集し、保存してください。

---

## チームで使う場合

- **共有ドライブ**（OneDrive、Google Drive、社内NASなど）に  
  `delivery-slip-organizer` フォルダを置く
- 全員のPCで同じ手順でセットアップ
- **1台だけ** `run.py` を起動して監視すれば、共有の `受け取り\` にPDFを入れるだけで処理可能

---

## 困ったときは

- エラーメッセージをそのままコピーして、社内の担当者やAIチャットに貼り付けて相談
- `エラー\error.log` に処理失敗の理由が記録されています
