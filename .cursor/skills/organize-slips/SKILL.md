# 納品書整理スキル

スキャンPDFの納品書を自動で分割・リネーム・振り分けする。

## トリガー

- 「処理して」「納品書を整理して」
- `受け取り/` にPDFを入れたあと

## 手順

1. `python3 run.py --once` で処理（または監視モードが起動中なら自動）
2. 結果を確認（成功件数・エラー件数）
3. エラーがあれば `エラー/error.log` を確認

## エラー対応

- ラベル名が合わない → `config.yaml` の `labels` を納品書の表記に合わせる
- 顧客フォルダ不一致 → `納品書/` にフォルダを追加
